SET(SOURCE_GROUP_BACKUP "${SOURCE_GROUP}")
SET(SOURCE_GROUP "${SOURCE_GROUP}/TempModule")
ADD_SUBDIRECTORY("${FORTE_EXTERNAL_MODULES_DIRECTORY}/TempModule" "${CMAKE_CURRENT_BINARY_DIR}/external/TempModule")
SET(SOURCE_GROUP "${SOURCE_GROUP_BACKUP}")
SET(SOURCE_GROUP_BACKUP "${SOURCE_GROUP}")
SET(SOURCE_GROUP "${SOURCE_GROUP}/Win_IX_QX")
ADD_SUBDIRECTORY("${FORTE_EXTERNAL_MODULES_DIRECTORY}/Win_IX_QX" "${CMAKE_CURRENT_BINARY_DIR}/external/Win_IX_QX")
SET(SOURCE_GROUP "${SOURCE_GROUP_BACKUP}")
